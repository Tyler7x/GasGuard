[package]
name = "gasguard-plugin-system"
version = "0.1.0"
edition = "2021"

[dependencies]
gasguard-rule-engine = { path = "../rule-engine" }
libloading = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
