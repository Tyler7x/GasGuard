# GasGuard Rule Testing Framework (Rust)

[package]
name = "gasguard-testing"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
gasguard-rules = { path = "../../packages/rules" }

[dev-dependencies]
